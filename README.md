# dotfiles
Dotfiles

## Testing local changes

Before pushing, test changes against the current state:

```bash
chezmoi --source=. diff
```

This shows exactly what would change without modifying anything.
